import wollok.game.*
import personaje.*
import nivel.*
import mirta.*

program game {
	game.height(13)
	game.width(13)
	nivel1.iniciar()
	macri.disparar()
	macri.moverAleatorio()
	game.start()
	
	game.showAttributes(persona)
}